我學到的重點
上游比下游更重要：強調不要把力氣放在生成後的補救，而是要在檢索前、中就把雜訊過濾掉。因為如果召回的知識本身就是錯的，再強的驗證器也只是精確地對照錯誤。
四模組閉環：Filter → Hybrid Retrieval → Control Flow → Validation。這種解耦設計讓每個模組專注在自己的責任，避免混亂。
雙層漏斗檢索：先用Hybrid Retrieval寬召回，再用Cross-Encoder精準篩選Top-5。這樣既能保留語意廣度，又能確保最後進入LLM的知識純度最高。
忠實度審查：下游不再盲判，而是把檢索到的chunks注入ValidationContext，嚴格比對答案是否真的有依據，避免幻覺。
優雅降級與短路機制：當知識不足時，不是無限重試，而是要能停下來，請使用者補料。這讓系統更誠實，也更省資源。
無向量環境的生存策略：即使不能用pgvector，也能靠Query Expansion、Keyword Density、Chunking等方法逼近效果。這提醒我，工具不是唯一解，思維才是。
三大品質指標：Context Precision、Context Recall、Faithfulness。這些量化指標讓系統優化有數字依據，而不是憑感覺。
三階段升級路徑：先打通基礎設施，再加上citation-grounding，最後才導入知識驅動路由。這種漸進式升級避免破壞既有穩定性。
我的想法
我覺得這節課的精神就是RAG的價值不在於讓AI更聰明，而在於讓錯誤更可控。  
它提醒我，工程師的責任不是追求完美答案，而是設計一個能在錯誤發生時停下來、診斷、修正的系統。
我特別喜歡要燃料，不丟稿這句話。意思是當知識不足時，不要硬生成，而是要誠實地停下來，請使用者補資料。這種設計讓AI更像一個可靠的助手，而不是一個隨便亂答的聊天機器人。
另外，無向量環境的降級策略也讓我很有感觸。很多時候我們會覺得沒有最新技術就做不出好系統，但這個課程告訴我：只要思維正確，即使在限制多的環境下，也能逼近理想效果。這是一種務實的工程智慧。
總結來說，我學到的核心是：現代 RAG 的重點在於上游治理、模組解耦、誠實降級，以及量化驗證。  
這些原則讓我重新思考，未來在設計系統時，不應只追求模型更強，而是要確保流程更穩。
